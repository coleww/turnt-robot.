<%= button_to "logout", '/session', method: :delete %>


Available classes for yr sick spans!
<ol>
  .center
  .left
  .right

</ol>

<%= form_tag 'status', method: :post do %>
  <%= text_area_tag :status, @user.status, escape: false, maxlength: 1024 %>
  <%= submit_tag "update status" %>
<% end %>